services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: graphic_designer
      POSTGRES_USER: payload
      POSTGRES_PASSWORD: payload_dev
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
