{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "_comment": "Non-secret defaults only. Secure parameters (postgresPassword, payloadSecret, acsConnectionString) and containerImage are passed by the deploy workflow from GitHub Actions secrets — never committed here.",
  "parameters": {
    "appName": {
      "value": "graphic-designer"
    },
    "environmentName": {
      "value": "production"
    },
    "location": {
      "value": "westeurope"
    },
    "emailFrom": {
      "value": "DoNotReply@example.com"
    },
    "contactEmailTo": {
      "value": "designer@example.com"
    }
  }
}
